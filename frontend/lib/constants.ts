/**
 * Shared application constants.
 *
 * Import from `@/lib/constants` — keep all magic strings here so they can be
 * updated in a single place.
 */

/** Primary support / contact email displayed across the frontend. */
export const SUPPORT_EMAIL = "info@leafxtract.com";
